/**
 * OF
 *
 * The of() method creates a new array instance from a variable number of arguments,
 * regardless of number or type of the arguments.
 * 
 * Parameters:
 * element0, ..., elementN: list of elements used to create the array
 *
 * The difference from Array(2) and Array.of(2) is that the first one creates an array with 2
 * slots empty and the second creates an array with one element, 2.
 */

console.log(Array.of(1)); // [1]


